#!/usr/bin/python3
# 9-max_integer.py
# Haile2723



def max_integer(my_list=[]):

    if len(my_lis) == 0:
        return (None)
    x = [x for x in sorted(my_list)]
    max_value = x[0]
    return (max_value)
